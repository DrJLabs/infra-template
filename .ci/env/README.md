This directory contains environment snapshots used for reproducibility.

- `kernel.json` records the output of `uname -a`.
- `docker.json` records the Docker version or `null` if Docker is not installed.

